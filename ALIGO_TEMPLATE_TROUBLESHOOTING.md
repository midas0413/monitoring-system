# Aligo 템플릿 오류 해결 가이드

## 문제
"메시지가 템플릿과 일치하지 않는다"는 오류가 계속 발생

## 템플릿 정보
- 템플릿 코드: `UC_2669`
- 템플릿 내용:
  ```
  #{시스템} 모니터링에 알림이 발생했습니다.
  알림내용 : #{알림}
  ```

## 현재 구현 (Python 코드 참고)

### Python 코드 방식
```python
'message_1': f"{system_info} 모니터링에 알림이 발생했습니다.\n알림내용 : {alert_content}",
'var1': system_info,
'var2': alert_content,
```

### Java 코드 (현재)
```java
String message1 = String.format("%s 모니터링에 알림이 발생했습니다.\n알림내용 : %s", systemInfo, alertInfo);
params.add("message_1", message1);
params.add("var1", var1Value);
params.add("var2", var2Value);
```

## 가능한 원인 및 해결 방법

### 1. message_1 파라미터 문제
**가능성 1**: `message_1`을 전달하지 않아야 함
- Aligo API가 템플릿 코드를 사용할 때 `message_1`을 무시하거나 오류를 발생시킬 수 있음
- **해결**: `message_1` 파라미터를 전달하지 않음

**가능성 2**: `message_1`에 템플릿 형식 그대로 전달해야 함
- 템플릿 변수 자리표시자를 그대로 사용: `#{시스템}`, `#{알림}`
- **해결**: `message_1 = "#{시스템} 모니터링에 알림이 발생했습니다.\n알림내용 : #{알림}"`

**가능성 3**: Python 코드처럼 실제 값으로 전달 (현재 방식)
- 실제 값으로 치환된 메시지 전달
- **현재 구현**: 이 방식 사용 중

### 2. 줄바꿈 문자 문제
- `\n` vs 실제 줄바꿈 문자
- **해결**: `\n` 사용 (현재 구현)

### 3. 템플릿 변수 순서 문제
- `var1` → `#{시스템}` (첫 번째 변수)
- `var2` → `#{알림}` (두 번째 변수)
- **확인 필요**: 템플릿에서 변수 순서 확인

### 4. 특수 문자 인코딩 문제
- 한글 인코딩 문제
- **해결**: UTF-8 인코딩 확인

## 디버깅 방법

### 1. 로그 확인
다음 로그를 확인하여 실제 전송되는 파라미터 확인:
```
Aligo 알림톡 API 파라미터 전송:
  tpl_code=UC_2669
  message_1=[실제 메시지]
  var1=[시스템명]
  var2=[알림내용]
```

### 2. Aligo API 응답 확인
`parseAlimtalkResponse()`에서 상세 오류 메시지 확인

### 3. 테스트 방법
1. `message_1`을 전달하지 않는 방법 시도
2. `message_1`에 템플릿 형식 그대로 전달하는 방법 시도
3. Python 코드와 정확히 동일한 방식으로 전달

## 권장 해결 순서

1. **현재 방식 유지** (Python 코드와 동일)
   - `message_1`에 실제 값 전달
   - `var1`, `var2` 전달

2. **message_1 제거 시도**
   - 템플릿 코드만 사용하고 `message_1` 전달 안 함

3. **템플릿 형식 그대로 전달**
   - `message_1`에 `#{시스템}`, `#{알림}` 자리표시자 그대로 전달
