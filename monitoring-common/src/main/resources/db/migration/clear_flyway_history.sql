-- ============================================================================
-- Flyway 스키마 히스토리 테이블 초기화 스크립트
-- ============================================================================
-- 다음 빌드 시 V1__init.sql이 적용되도록 모든 버전 이력 삭제
-- 
-- 사용 방법:
-- 1. 이 스크립트를 PostgreSQL DB 클라이언트에서 직접 실행합니다.
-- 2. 실행 후 다음 애플리케이션 빌드/실행 시 Flyway가 V1__init.sql을 처음부터 적용합니다.
--
-- 주의사항: 
-- - 이 스크립트는 수동으로 실행해야 합니다 (Flyway가 자동으로 실행하지 않습니다).
-- - 프로덕션 환경에서는 신중하게 사용하세요.
-- - 모든 마이그레이션이 V1__init.sql에 통합되어 있어야 합니다.
-- - 실행 전에 데이터베이스 백업을 권장합니다.
-- ============================================================================

-- Flyway 히스토리 테이블의 모든 레코드 삭제
DELETE FROM flyway_schema_history;

-- 확인: 삭제 후 테이블이 비어있는지 확인
-- SELECT COUNT(*) FROM flyway_schema_history;  -- 결과가 0이어야 함

-- 참고: 
-- - 다음 빌드 시 Flyway가 V1__init.sql을 실행하고 flyway_schema_history에 기록합니다.
-- - V1__init.sql에는 다음 내용이 모두 통합되어 있습니다:
--   * 초기 스키마 (servers, monitoring_rules, check_runs 등)
--   * VPN check_method 컬럼 (V6)
--   * 카카오 템플릿 코드 컬럼 (V9)
--   * 카카오 템플릿 관리 테이블 (V10)
